import clsx from "clsx";
import React from "react";

const Header = () => {
  return (
    <div className={clsx("flex justify-start font-bold")}>
      Nextjs Starter project
    </div>
  );
};

export default Header;
